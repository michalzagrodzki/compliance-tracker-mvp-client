 
// Re-export all utilities from the shared utils for backward compatibility
export {
  getStatusIcon,
  formatSimilarity,
  formatBasicContent,
  formatNumberedSection,
  formatMessageContent,
  formatImplementationStep,
  formatBulletPoints,
  formatInlineContent,
  formatRecommendationContent,
  renderGapSourceDocuments as renderSourceDocuments,
} from '@/components/shared/utils';